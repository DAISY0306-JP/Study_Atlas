PORT=3000

# Supabase project settings (Project Settings > API)
# anon key only — RLS + the caller's access token enforce per-user access,
# so the service role key (which bypasses RLS) is not used by this API.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
